{
  "version": 3,
  "buildCommand": "npm install -g @angular/cli && ng build --configuration production",
  "outputDirectory": "dist/kopiBudget.ui",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
